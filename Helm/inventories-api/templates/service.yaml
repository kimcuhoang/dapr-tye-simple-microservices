apiVersion: v1
kind: Service
metadata:
  name: {{ include "inventories-api.fullname" . }}
  labels:
    {{- include "inventories-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inventories-api.selectorLabels" . | nindent 4 }}
