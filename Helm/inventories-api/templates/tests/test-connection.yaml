apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "inventories-api.fullname" . }}-test-connection"
  labels:
    {{- include "inventories-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "inventories-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
