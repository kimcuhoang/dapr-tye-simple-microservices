apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "product-catalog-api.fullname" . }}-test-connection"
  labels:
    {{- include "product-catalog-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "product-catalog-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
