apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "simplestore-sqlserver.fullname" . }}-test-connection"
  labels:
    {{- include "simplestore-sqlserver.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "simplestore-sqlserver.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
